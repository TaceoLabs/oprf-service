function {{ function_name }}Round2Contribution() internal pure returns (Types.Round2Contribution memory) {
    Types.SecretGenCiphertext[] memory ciphers = new Types.SecretGenCiphertext[](3);
    ciphers[0].cipher = {{ cipher0 }};
    ciphers[0].nonce = {{ nonce0 }};
    ciphers[0].commitment = Types.BabyJubJubElement({
        x: {{ commitment0x }},
        y: {{ commitment0y }}
    });

    ciphers[1].cipher = {{ cipher1 }};
    ciphers[1].nonce = {{ nonce1 }};
    ciphers[1].commitment = Types.BabyJubJubElement({
        x: {{ commitment1x }},
        y: {{ commitment1y }}
    });

    ciphers[2].cipher = {{ cipher2 }};
    ciphers[2].nonce = {{ nonce2 }};
    ciphers[2].commitment = Types.BabyJubJubElement({
        x: {{ commitment2x }},
        y: {{ commitment2y }}
    });

    return Types.Round2Contribution({
        compressedProof: [{{ proof }}],
        ciphers: ciphers
    });
}